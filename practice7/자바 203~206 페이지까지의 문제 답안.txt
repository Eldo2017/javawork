문제 1. 3) 로컬 변수 (클래스의 구성 멤버로 틀린 것)
문제 2. 4) 하나의 클래스는 하나의 객체만 생성 가능하다 (객체에 대해 틀린 것)
문제 3. (Person 클래스 작성하기)

자료형			필드명			설명
String			name			이름
String			gender			성별
int			age			나이
int			height			키
int			weight			체중

class Person {
 	String name = "심영"; // 이름
	String gender = "Male"; // 성별
	int age = 47; // 나이
	int height = 166; // 키
	int weight = 60.9; // 체중
}

문제 4. 2) 생성자는 오버로딩할 수 없다. (생성자에 대해 틀린 것)
문제 5. 3) 매개변수는 있을 수도 있고 없을 수도 있다. (메서드에 대해 틀린 것)
문제 6. (메서드 오버로딩의 의미)
메서드 오버로딩은 메서드로 사용하는 함수 이름은 같은데 변수로 쓰이는 자료형이나 그 변수의 개수를 다르게 하여 만드는 것이다.
문제 7. println() (나중에 실행이 종료되는 메서드)
문제 8. 123456 (예제 프로그램 실행 결과)
문제 9. (Exercise 클래스를 싱글톤으로 완성하기)

class Exercise {
	private static Exercise ex = new Exercise();
	
	private Exercise(){
		System.out.println("객체를 생성합니다.");
	}
	
	static Exercise getInstance() {
		return ex;
	}
}

문제 10. this (클래스 내부에서 자기 주소를 참조하는 예약어)
문제 11. (Math 클래스를 작성하고 객체를 생성해 메서드 호출하기)

class Math {
	// 최대값 반환 메서드
	public int max(int[] arr) {
		int max = arr[0]; // 맨 첫번째 값을 최대값으로 설정
		for(int i=1;i<arr.length;i++) {
			if(arr[i] > max) max = arr[i];
		}
		return max;
	}
	
	//최소값 반환 메서드
	public int min(int[] arr) {
		int min = arr[0]; // 맨 첫번째 값을 최대값으로 설정
		for(int i=1;i<arr.length;i++) {
			if(arr[i] < min) min = arr[i];
		}
		return min;
	}
}

문제 12. 3) 접근 제한자는 클래스나 필드, 생성자, 메서드에 지정할 수 있다. (접근 제한자에 대해 틀린 것)
